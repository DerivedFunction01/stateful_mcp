{
	"name": "@stateful-mcp/clinical",
	"version": "0.1.2",
	"description": "Clinical domain engines and resource loaders for Stateful framework",
	"keywords": [
		"clinical",
		"ucum",
		"vitals",
		"observation",
		"stateful",
		"mcp"
	],
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "bun build ./src/index.ts --outfile dist/index.js --target bun --format esm --external '*.node' && tsc --emitDeclarationOnly --declaration --outDir dist",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@duckdb/node-api": "^1.5.4-r.1",
		"@stateful-mcp/core": "workspace:^",
		"@sqlite.org/sqlite-wasm": "latest"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "^20",
		"typescript": "^5"
	}
}
