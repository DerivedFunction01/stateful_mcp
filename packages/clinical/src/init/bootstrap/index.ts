// export { bootstrapClinicalStores } from "./bootstrap-writer";
export type { BootstrapStores } from "./stores";

// TODO(v2-bootstrap): the V2 builder/runtime owns initialization. These
// retained store interfaces only cover non-parser reference data while the
// old seed orchestrator is removed.
