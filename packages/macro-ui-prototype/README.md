# Macro UI Prototype

This package is a fixture-driven visual reference for the generic macro workspace. It deliberately does not import the clinical package or the legacy CLI.

Run it with:

```bash
bun run dev
```

The fixture switcher demonstrates core, retail, engineering, and clinical-shaped contribution layouts. The prototype supports lightweight tab, panel, palette, dropdown, focus, and diagram interactions; it does not execute macros or persist data.
