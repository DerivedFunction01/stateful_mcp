{
	"name": "@stateful-mcp/macro",
	"version": "0.1.0",
	"private": true,
	"description": "Topic-agnostic macro contracts and parsing foundation",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "bun build ./src/index.ts --outfile dist/index.js --target bun --format esm && tsc --emitDeclarationOnly --declaration --outDir dist",
		"check": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@stateful-mcp/core": "workspace:^"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	}
}
